# 01 · worktree 역할 분리 오케스트레이션

**한 줄**: 구현 역할 둘을 병렬로 돌리고, 통합·검증 역할이 그 위에 쌓이며, 승인은 사람만 하는 5역할 파이프라인.
**출처**: 실습 — <멀티에이전트 커맨드센터 및 통합 오케스트레이션> 교육 과제. starter 프로젝트가 제공됨
**상태**: 실험 중
**포팅**: 없음. Claude Code에도 옮길 수 있으나 아직 안 해봄

이 항목은 **설계만 기록한다.** starter 코드와 과제 데이터는 담지 않는다. 교육 과정에서 받은 자료라 재배포 조건이 확인되지 않았고, 재현에 필요한 것은 아래 구조이지 애플리케이션 코드가 아니다.

## 용도

한 작업을 여러 에이전트에 쪼개되, **자기가 만든 것을 자기가 통과시키는 경로를 막고 싶을 때** 쓴다.

쓰면 안 되는 경우: 작업이 진짜로 병렬이 아닐 때. 아래 구조는 역할 사이 파일 충돌이 실재할 때 값을 하고, 그렇지 않으면 branch 관리 비용만 남는다. 문서 저장소처럼 충돌이 없는 곳에는 과하다.

## 전제

- 하네스: Codex Desktop. 작업 트리를 하네스가 직접 만들고 관리한다(managed worktree)
- 사람이 `git worktree add` / `remove`를 실행하지 않는 것이 규칙이다. 손으로 만든 작업장은 하네스가 추적하지 못한다
- 검증 스크립트가 있어야 한다. 이 설계의 판정은 전부 스크립트 통과 여부에 걸려 있다
- 실습에서는 외부 의존성을 금지하고 표준 라이브러리만 쓰도록 제약했다. 검증이 어느 환경에서나 같은 결과를 내게 하려는 것이다

## 설계

### 역할과 branch 그래프

```
master ─┬─> task/003-backend ──┐
        │                      ├─> task/003-integration ─> task/003-tester
        └─> task/003-frontend ─┘                                  │
                                                                  v
                                              사람이 local master에서 최종 merge
```

- **Backend / Frontend** — master에서 각각 출발해 동시에 구현. 서로를 기다리지 않는다
- **Integration** — backend branch에서 출발해 frontend를 `--no-ff`로 병합. 구현 파일은 건드리지 않고 통합과 전체 검증만 한다
- **Tester** — integration 위에서 검증. **코드 수정 권한이 없다**
- **Main(오케스트레이터)** — 분해와 조율만. 코드를 짜지 않고 commit도 하지 않는다. 증거를 모아 사람에게 판단을 제안한다

순수 fan-out이 아니다. 앞의 둘은 병렬이지만 integration은 양쪽이 끝나야 시작된다.

### 권한을 프롬프트가 아니라 데이터로 선언

`tasks/acceptance.json` 하나가 채점 기준과 역할 권한을 모두 소유하고, 어떤 역할도 이 파일을 수정할 수 없다. 담고 있는 것은 대략 이런 것들이다.

| 항목 | 내용 |
|---|---|
| 판정 규칙 | 우선순위 분류 규칙, 필수 필드, 허용값 목록 |
| 케이스 | 입력과 기대 출력 쌍. API 검증이 이걸로 돈다 |
| UI 계약 | 필수 DOM id, 필수 필터 목록 |
| `allowedFilesByRole` | 역할별로 **수정 가능한** 파일 목록 |
| `forbiddenFilesByRole` | 역할별로 **금지된** 파일 목록 |
| 위험도 정책 | 검증 전부 통과면 LOW, 하나라도 실패면 HIGH |

핵심은 마지막 두 줄이 아니라 **형식**이다. "너는 server.py만 고쳐라"를 프롬프트로 지시하면 지켜졌는지 사람이 읽어서 판단해야 하지만, 데이터로 선언하면 `audit.py`가 diff를 대조해 기계로 판정한다. 규칙을 어겼는지가 의견이 아니라 사실이 된다.

### 증거 규약

- 구현 전달은 **branch와 commit**이다. 별도 patch 파일을 만들지 않는다 — diff가 이미 변경 증거다
- 판단 전달은 **역할별 report 파일**이다. `docs/<task>/<role>/report.md`로 경로가 고정돼 있어 어느 역할의 주장인지 섞이지 않는다
- Tester만 `approval_draft.json`을 쓴다

### 승인 게이트

- approval draft의 `status`는 **항상 `pending`**이다. 에이전트가 승인 상태를 쓰는 것 자체가 금지다
- 최종 merge는 사람이 local master에서만 한다. 에이전트는 `push`하지 않는다
- **검증 스크립트나 채점 기준을 고쳐서 통과시키는 것을 명시적으로 금지**한다. 이게 없으면 "통과"가 무의미해진다

## 이 설계의 요점

1. **판정자와 구현자를 분리한다.** Tester는 코드 수정 권한이 없고 Main은 구현하지 않는다. 자기 작업을 자기가 승인하는 경로가 구조적으로 없다.
2. **제약을 검증 가능한 형태로 둔다.** 권한도, 채점도 데이터 파일에 있고 스크립트가 판정한다.
3. **자동 승인을 금지한다.** 에이전트는 `pending`까지만 만들고 사람이 게이트를 넘긴다.

1번과 3번은 이 레포의 다른 항목과도 통한다. `상태: 미검증`을 스스로 `검증됨`으로 바꾸지 않는 것과 같은 원리다.

## 결과

실습에서 실제로 돌려봤으나 관찰 내용이 아직 기록되지 않았다. 아래를 채우면 `검증됨`으로 올린다.

- **잘 된 점**:
- **실패한 점**: 어느 역할에서 막혔는지, 권한 선언이 실제로 지켜졌는지(`audit.py`가 위반을 잡았는지)
- **사람 승인 게이트**: 실제로 걸렸는지, 아니면 형식적으로만 존재했는지
- **병렬의 실익**: backend/frontend 동시 진행이 실제로 시간을 줄였는지, 아니면 integration 대기에 흡수됐는지
- **다음에 바꾼다면**:
