# NN · 설계 이름

**한 줄**: 이 하네스가 하는 일을 한 문장으로. 여기만 읽고 넘길지 말지 판단할 수 있게.
**출처**: 자작 — 또는 외부면 `저자 / URL`
**상태**: 검증됨 | 실험 중 | 미검증 | 폐기
**포팅**: 다른 하네스에 같은 설계가 있으면 `codex/02-...`, 없으면 이 줄을 지움

외부 출처면 아래 두 줄을 추가합니다. 원문은 계속 바뀌므로 언제 시점의 것인지 남겨야 합니다.

**스냅샷**: `YYYY-MM-DD`, 커밋 `abc1234`
**라이선스**: MIT | Apache-2.0 | 명시 없음 — 명시 없으면 원문을 담지 말고 링크와 발췌만 둔다

## 용도

어떤 상황에서 이걸 꺼내 쓰는가. 반대로 **언제 쓰면 안 되는지**도 적어두면 나중에 잘못 고르는 걸 막습니다.

## 전제

- 하네스 / 모델:
- 필요한 툴·권한:
- 레포·환경 조건:

전제가 안 맞으면 그대로 복사해도 재현되지 않습니다. 나중에 "왜 안 되지"의 절반은 여기서 갈립니다.

## 구성

| 파일 | 역할 |
|---|---|
| `prompt.md` | |
| `strategy.md` | |
| `settings.json` | |

실제로 있는 파일만 남기고 나머지 행은 지웁니다.

## 결과

- **잘 된 점**:
- **실패한 점**:
- **다음에 바꾼다면**:

이 섹션이 비어 있으면 항목을 추가한 의미가 없습니다. 한 번이라도 돌려본 뒤에 채웁니다.

외부 출처를 아직 안 써봤다면 `상태: 미검증`으로 두고 이 섹션은 비워둡니다. 남의 평판을 내 검증 결과로 옮겨 적지 않습니다.
