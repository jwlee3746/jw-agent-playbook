.DS_Store
.env
*.local

# 라이선스가 명시되지 않은 외부 원문과 그 번역본을 담는 로컬 전용 디렉토리.
# 개인 참고용으로 손에 두되, 재배포가 되지 않도록 커밋하지 않는다.
_local/

# 루트 CLAUDE.md는 지금 실제로 적용 중인 규칙이라 로컬에서만 관리한다.
# 항목으로 남길 규칙은 claude/NN-*/ 안에 언어 접미사를 붙여 커밋한다.
/CLAUDE.md
